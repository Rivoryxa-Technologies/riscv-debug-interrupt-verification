# Contributing

Run `./setup.sh` and require both the correct-source pass and named negative-control failure. Keep the upstream revision immutable, do not commit fetched or generated files, and update the documented assumptions whenever the testbench environment changes.
